# Vite
# Dev server: extra allowed hosts (comma-separated, e.g. 100.64.0.1)
VITE_DEV_ALLOWED_HOSTS=100.103.76.82

# E2E (Playwright / Tevm; ports can vary per deploy)
PUBLIC_E2E_TEVM=
PUBLIC_E2E_TEVM_RPC_URL=

# LLM (hosted fallback for dialogue/explain)
PUBLIC_LLM_ENDPOINT=
PUBLIC_LLM_API_KEY=

# OpenCode Zen (free models; client calls via proxy when PUBLIC_OPENCODE_ZEN_API_KEY set).
# Get key: https://opencode.ai/auth — sign in, add billing, copy API key.
# E2E: set to any non-empty value (e.g. e2e) at build so agent-chat tests run; tests mock the proxy so no real key is used.
PUBLIC_OPENCODE_ZEN_API_KEY=

# PartyKit (rooms; host/port can vary per deploy)
PUBLIC_PARTYKIT_HOST=

# Stork (prices; proxy uses STORK_REST_TOKEN server-side). Obtain token: team@stork.network or @StorkOracle
PUBLIC_STORK_REST_TOKEN=
STORK_REST_TOKEN=
