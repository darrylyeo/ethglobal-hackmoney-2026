# Build re-verify: spec 001 Constants and networks

- All specs complete; re-verification mode.
- Picked spec 001 (Constants and networks).
- Verified: NetworkType, ChainId, networks, networksByChainId, ercTokens, ercTokensBySymbolByChainId; code style (tabs, no semicolons, as const satisfies).
- `pnpm test:unit`: passed (Deno + Vitest).
- No regressions. DONE.
